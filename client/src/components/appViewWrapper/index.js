import React from 'react';
import { StyledAppViewWrapper } from './style';

const AppViewWrapper = (props) => {
    return (
        <StyledAppViewWrapper {...props} />
    );
};

export default AppViewWrapper;